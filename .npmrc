# Use hoisted node-linker for React Native compatibility
node-linker=hoisted

# Hoist all dependencies to avoid Metro resolution issues
shamefully-hoist=true

# Auto install peer dependencies
auto-install-peers=true
